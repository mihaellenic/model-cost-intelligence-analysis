---
description: Orient the Product Owner agent on first activation — inventory what product artifacts exist vs. are missing, and surface the top ambiguities to resolve.
agent: po
---

Orient yourself in this repository and report back.

1. Read `README.md` and any `AGENTS.md` if present. Summarize what this project is *as built today* (the PoC), separate from any aspirations.
2. Check for existing product artifacts under `docs/product/` (vision.md, personas.md, backlog.md, decision-log.md, assumptions.md). Inventory which exist and which are missing.
3. Identify the **top 3 ambiguities** you see right now — dimensions where a decision is needed before serious restructuring can proceed (audience, scope, data freshness model, success metric, positioning vs. alternatives, etc.). For each, state the ambiguity precisely, the cost of not resolving it, and a recommended default.
4. Propose which product documents should be created first and in what order, based on the gap analysis.

Present this as a short orientation report. Do NOT create files yet — first present the report and ask whether to proceed with creating the initial document set.

$ARGUMENTS