# Assumptions

Assumptions are liabilities. Each has an expiry and a "validate by" trigger. An expired assumption is a bug — either re-validate or convert to a decision.

| ID | Date | Assumption | Why deferred | Expiry / validate-by | How to validate |
|---|---|---|---|---|---|
| ~~A1~~ | 2026-08-29 | ~~The pair can be ranked by independent per-role cost first; expected-$ across a task mix is a later refinement (decision D3).~~ | ~~We don't yet have a task taxonomy or default task mix (backlog B2). Shipping the pair on per-role cost is a provable improvement on the current single-pick and unblocks B1.~~ | **Retired 2026-08-29** — B2 landed; D3 updated to adopt expected-$ directly. | Retired: the per-role-first deferral is no longer the shipping path. |
| ~~A2~~ | 2026-08-29 | ~~A 2-role split (planning vs execution) is sufficient to model a coding-agent workflow.~~ | ~~It's the simplest model that matches the user's stated decision (D2).~~ | **Retired 2026-08-29** — resolved by B2. A 3rd task type (VERIFICATION) is warranted; see decision D6 and `b2-taxonomy.md`. | Retired: both evidence streams independently surfaced a 3rd tier. |
| ~~A3~~ | 2026-08-29 | ~~benchlm.ai remains the right intelligence source for the mature version.~~ | ~~It's the current source (README). No alternative has been evaluated; switching is out of scope until the pair output ships.~~ | **Retired 2026-08-30** — superseded by D12: OpenRouter `/api/v1/benchmarks` becomes the sole primary intelligence source, eliminating cross-source joins (the location of every past integrity defect). benchlm's static data API is retained as an optional cross-check only. | Retired: the join-elimination argument and the benchlm static-API verification (400 models / 224 scored) made single-sourcing from OpenRouter strictly better. |
| A4 | 2026-08-29 | The median-line quadrant framing is legible to users as the *reasoning* under a pair recommendation (D5). | It's the current substrate and works for the PoC. No usability evidence either way for the mature version. | First usability check on B1's pair output (B1's success metric includes a 5-of-5 internal trial). | If ≥3 of 5 trial users can't explain the pair from the quadrants, D5 flips and we redesign the reasoning surface. |
| A5 | 2026-08-29 | API pricing (cheapest OpenRouter provider) is a faithful proxy for the solo dev's actual spend. | README §Caveats already flags real costs vary with traffic/batching/negotiated rates. Good enough to ship; not good enough to claim precision. | When a user reports the recommendation's expected $ diverges from their actual bill by >2×. | Add a "your actual cost may vary" caveat near the pair output; long-term, consider a cost-range instead of a point price. |
| A6 | 2026-08-29 | A 3rd *task type* (VERIFICATION) does not force a 3rd *recommended model*. Verification is often deterministic ($0) or uses the execution model; the recommendation output stays a pair (D2 holds). | Introduced during B2 reconciliation. The taxonomy has 3 cost line items but the user-facing output is still 2 models. | When evidence shows users' verification step is model-heavy AND needs a model distinct from execution. | Track in B1 usability: if users ask "what model should I use for review?" frequently, D2 reopens toward a tiered set. |
| A7 | 2026-08-29 | The default task mix of ~65/30/5 is a reasonable starting point despite high session-to-session variance (PLANNING 40–99%, EXECUTION 0–57%). | The variance is real (local evidence); a fixed default will be wrong for many users. Defensible as a starting point only because B5 (adjustable slider) is promoted to "next". | B1 ships. If B5 is not ready when B1 ships, the default becomes a liability. | B1 ships with the mix as a visible adjustable parameter, not a hidden constant. |
| A8 | 2026-08-30 | AA's free tier will expose `coding_index` and `agentic_index` widely enough for B6's per-task-type fitness. | Free-tier probe: coding_index present for only 72/624 models, agentic for 54/624 (11%/8.6%) — the breadth lives behind AA's Pro tier. B6 is "later" and not blocked today. | When B6 moves to "now". | Options at that time: AA Pro tier (paid), OpenRouter benchmarks feed (has coding/agentic per its docs), or a third source. Proactively re-probe rather than assume. |