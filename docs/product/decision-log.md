# Decision log

| ID | Date | Context | Options | Choice | Rationale | Revisit trigger |
|---|---|---|---|---|---|---|
| D1 | 2026-08-29 | Who is the intended user of the mature version? Solo dev, team lead, or self-hoster? | (a) Solo agent user optimizing spend; (b) Team lead doing build-vs-buy; (c) Self-hoster doing GPU TCO. | (a) Solo agent user. | User input: "intended user is a user of opencode or other coding agent; goal is cost optimization — which models are the most cost effective for different types of tasks." (b) and (c) are different products; admitting them would split focus. | A funded team-use request that can't be served by the solo framing. |
| D2 | 2026-08-29 | What is the target output of the product — a single best model, or something else? | (a) Single best model (current quadrant picks); (b) A recommended **pair**: one model for planning/complex work, one for execution/simpler tasks; (c) A full tiered set (≥3 models). | (b) A recommended pair. | User input: "a solo dev can pick a set of 2 different models — one for planning and complex work, the other for simpler tasks or implementation work." This is the unit of decision the user actually makes. | Evidence users routinely want a 3rd tier (e.g. a "trivial" tier for completions); then promote to (c). |
| D3 | 2026-08-29 | How is the pair ranked — independent cheapest-per-role, or expected $ across a task mix? | (a) Independent: cheapest planning-model + cheapest execution-model, ranked separately; (b) Expected $ across a representative task mix (e.g. 20% planning tokens / 80% execution). | (b) Expected $ across the task mix. | Originally deferred (ship (a) first, graduate to (b)) because B2 hadn't landed. **B2 landed 2026-08-29** — taxonomy + default mix are now available (`b2-taxonomy.md`). B1 adopts (b) directly; the per-role fallback (a) is no longer the shipping path. | If B1's usability shows the expected-$ math is illegible to users, fall back to presenting the pair with per-role cost alongside, not instead of, the workflow cost. |
| D4 | 2026-08-29 | Does the existing README-encoded "data integrity over coverage" rule still hold for the mature version? | (a) Keep: a wrong price is worse than a missing one; (b) Relax to favor coverage. | (a) Keep. | Unchanged from README §Data-integrity-rules. A fabricated "sweet spot" outlier (e.g. from a `:free` variant) is strictly worse than an empty quadrant for a cost-optimization tool — it produces a wrong *spend* recommendation, the core output. | Never, unless the source data becomes auditable end-to-end. |
| D5 | 2026-08-29 | Is the median-line quadrant framing still the right substrate for the mature version? | (a) Keep quadrants as the substrate; (b) Replace with a direct expected-$ surface. | (a) Keep, as substrate only. | The pair output (D2) is the primary surface; quadrants become the reasoning layer under it. Replacing them now is rework without evidence the new surface is clearer. | A usability test where ≥3 of 5 users can't explain *why* a pair was recommended from the quadrants. |
| D6 | 2026-08-29 | Does the B2 task taxonomy use 2 or 3 top-level task types? | (a) 2 types (PLANNING, EXECUTION) — A2 holds; (b) 3 types with TRIVIAL as the 3rd; (c) 3 types with QA as the 3rd; (d) 3 types with VERIFICATION as the 3rd. | (d) 3 types: PLANNING, EXECUTION, VERIFICATION. | Both evidence streams independently surfaced a 3rd tier, retiring A2. TRIVIAL rejected (3% volume, no distinct fitness dimension). QA rejected as a separate tier — folded into PLANNING because its model-selection dimension (long-context reasoning) is identical; folding it into execution would mislabel the reasoning load. VERIFICATION chosen because it affects the cost model (failed verification → re-run → more tokens), is externally validated (Cursor's judge, VS Code's review stage, Code Review Agent Benchmark), and does not force a 3rd recommended model (often $0 deterministic or billed to execution model — see A6). | Evidence that users' verification step is model-heavy AND needs a distinct model → reopens D2 toward a tiered set (c). |
| D7 | 2026-08-29 | What is the default task mix for B1's expected-$ pair ranking? | (a) Use the literature proxy (Khojah: ~12% planning); (b) Use the local token-weighted aggregate (~65% planning); (c) No default, slider-only. | (b) Local token-weighted aggregate, as a visible adjustable parameter (not a hidden constant). | The literature proxy is dialogue-counted from non-agent ChatGPT sessions — wrong unit for a per-token cost tool. Coding agents read massive tool_results that inflate planning's token share far beyond its dialogue-count share. The local number is token-weighted and from actual coding-agent sessions. But the variance (PLANNING 40–99%) is too high for a hardcoded constant — hence B5 promoted to "next" and the mix must be user-adjustable from B1's first version. | B1 usability shows users can't find or don't understand the mix control → redesign the control, not the default. |
| D8 | 2026-08-29 | What is the source of truth for the model list — the hand-maintained curated-list.json (model-level), or OpenRouter's API (family-level allowlist)? | (a) Keep curated-list.json as model-level whitelist + PricingMap bridge; (b) OpenRouter API as source of truth, filtered by a family-allowlist, metadata derived from model names. | (b) OpenRouter as source of truth. | User input: "include all coding models; auto-discovery, not hand-maintenance." Removes PricingMap (the fragilest, most error-prone part of the pipeline — D4's biggest risk surface). Removes fetch-ollama.js (dead). Shrinks curation from ~150 model-level entries to ~25 family-prefix rules. Auto-captures new variants within known families without manual edits. Cost: `size_gb` and `ollama_available` fields become meaningless and are removed; `params_b`/`family`/`tier` must be derived from model names (new code, new test surface → B4 scope expands). | If OpenRouter's API becomes unreliable or changes its ID naming convention such that family-derivation breaks silently. Validate with B4 tests on every `npm run data`. |
| D9 | 2026-08-29 | What's the coding-relevance filter for the OpenRouter-as-source-of-truth pipeline? | (a) Family-allowlist (~25 prefixes, human-curated); (b) Category-blocklist (exclude known non-coding); (c) Hybrid (both). | (a) Family-allowlist. | Most defensible — every model in the product is there because a human decided its family is coding-relevant, keeping D4 honest. OpenRouter doesn't tag categories cleanly, so a blocklist (b) would let misclassified non-coding models contaminate the quadrant math. The cost of (a) is "add a family prefix when a new coding-relevant family appears" — ~2-3 times/year. | A new major coding-model family appears that isn't covered by the allowlist → add it. This is maintenance, not a design failure. |
| D10 | 2026-08-30 | Should `params_b` survive in the cloud-only product, given B9 exposed a `null <= 7` bug in the "Best small" pick? | (a) Fix B9 (exclude null params from the small-model pick); (b) Keep params as decoration and fix; (c) Remove params entirely: `params_b`/`tier` fields, `deriveParamsB`/`deriveTier` functions + tests, bubble-size encoding, max-params filter, "Best small" pick. | (c) Remove entirely. | Parameter count is a vestige of the Ollama era — it answered "can my GPU run this," which is out of scope per vision. For a cloud user it's a *proxy* for two things the product already measures directly (intelligence axis, cost axis), so it adds no independent decision information. Empirically 167/213 (78%) models have `params_b: null` — systematically missing for exactly the closed frontier models that fill the planning slot. No planned backlog item (B1, B5, B6) uses it. Deleting the "Best small" pick is strictly better than fixing B9's null-handling. B1 builds on a params-free data model. | If a genuinely decision-relevant, OpenRouter-provided dimension emerges — candidate: `context_length`, which every model has and coding-agent users care about (large codebases need large windows). Parked for discovery, not committed. |
| D12 | 2026-08-30 | What is the intelligence data source — benchlm (scraped HTML or static data API), or OpenRouter's `/api/v1/benchmarks`? | (α) benchlm data API only, with hardcoded ID mapping; (β) benchlm now + OpenRouter benchmarks as second source; (γ) full multi-source stack. | **None of the above — OpenRouter benchmarks as the sole primary intelligence source** (a stronger cut of β, proposed by the user). | The project's entire defect history (family-aggregate fabrication, 168-vs-224 scrape loss, pagination gap, old PricingMap fragility) lives at *cross-source joins*, not in sources. Sourcing catalog, pricing, and intelligence from the same API eliminates the join entirely: matching is by exact OpenRouter ID, no fuzzy matching, no family-pattern regexes, no hardcoded mapping table. Also delivers `coding_index`/`agentic_index` which map onto the B2 taxonomy and unblock B6's per-type fitness later. Accepted costs: single-vendor dependence (mitigation: benchlm static data API retained as optional cross-check; per-record `intelligence_source` field keeps a swap possible); scores become aggregated composites (attribution + caveat in README); `/api/v1/benchmarks` requires an API key (401 verified without one). Models without benchmark data → `intelligence: null`, gray on scatter, excluded from pair card (D4 corollary: a wrong recommendation is worse than no recommendation). | If OpenRouter benchmark coverage of our catalog proves poor (<60% of tracked models with indexes) in the B11 probe report, fall back to (α)/(β) with benchlm's verified static data API (`benchlm.ai/data/leaderboard.json`, 400 models / 224 scored). Also revisit if OpenRouter changes the endpoint's terms or shape. |
| D13 | 2026-08-30 | Corrections to the analysis record (no product change — an honesty entry). | — | Two claims retracted: (1) "benchlm has only 168 entries / never benchmarked Luna" — wrong; benchlm's static data API serves 400 models / 224 scored (verified via benchlm.ai/data/models.json + leaderboard.json); our `__NEXT_DATA__` scrape was the truncated view, and the scraper's `rankingEligible` filter dropped zero scored models (all 128 dropped entries had no score). (2) "Scraper under-collects" — wrong about the filter, right about the total: the gap was the HTML embed, not the filter. The user's challenge ("benchlm has 400") and the user's OpenRouter-benchmarks research triggered both corrections and led to D12. | Scraper evidence and its corrections are recorded here and in `b13-report.md` (agent's coverage report). | — |
| D14 | 2026-08-30 | B11 landed at 8.9% exact coverage (19/213): OpenRouter's benchmark feed keys by dated `model_permaslug` (`gpt-5.6-sol-20260709`) while the catalog keys by undated ID, and only the `artificial-analysis` source (115/1449 entries) carries intelligence indexes. The cross-source-join defect D12 aimed to kill reappeared *inside* one vendor. AA's free API (`/api/v2/language/models/free`, verified: 624 models / 611 scored, pagination, key-gated but $0) was probed as the alternative: 55% exact slug-match coverage (58.2% with catalog-side date stripping), per-model variant scores (Luna 52.3 ≠ Sol 60.9 — the original defect cured), and `coding_index`/`agentic_index` pass-through (sparse in free tier: 72/54 models — recorded as A8). Comparability check killed the naive hybrid: benchlm vs OpenRouter/AA joined across 53 shared models showed mean delta −20.1, r = 0.794 with per-model spread −1.5 to −32.6 — the platforms measure differently; scores must NEVER be mixed on one axis. | (a) OpenRouter-only + dated-suffix canonicalization (~30%); (b) naive hybrid (benchlm + OpenRouter blended) — rejected by comparability data; (c) AA free API as sole intelligence source with deterministic join + explicit effort-variant policy. | (c) AA free API as the sole intelligence source. | Highest genuine coverage (~58% measured pre-collapse, >60% expected post-D15); per-model scores kill the family-stamp defect class; deterministic normalizable join; attribution required (README + footer). Effort-variant policy (decided): AA's un-suffixed plain base wins when present; if only effort variants (`-low/-medium/-high/-xhigh`) exist → median + `intelligence_scope: "effort-median"`, never a silent pick (spread within a family reached 20+ points in the probe). Legacy `-pro` models (o1-pro, gpt-5-pro, 5.4/5.5/5.2-pro) stay distinct — genuinely different products. Catalog-side date stripping on OpenRouter IDs is allowed; AA-side date reasoning is FORBIDDEN (vendors date the same generation differently — deepseek-v4-pro-0813 vs AA's 0424 variants at different scores). Collisions after normalization → null + report, never a guess. | Coverage at implementation <60% → B12 reports the honest number; monitor and revisit. AA changes the free-tier shape/fields → re-probe. OpenRouter benchmarks API remains the documented fallback (fetcher retained as auxiliary script, out of the default pipeline). |
| D15 | 2026-08-30 | Are "Pro"/"Fast" catalog variants distinct models? (Triggered by user hypothesis that GPT pros are subscription-plan flavors, not models.) | (a) Treat as distinct models (status quo — produced fake runner-up pairs, B10); (b) Collapse sibling variants into their base; (c) Drop them from the catalog entirely. | (b) Collapse — with a machine-readable rule. | Validated against primary sources: OpenRouter's own descriptions state "GPT-5.6 Luna Pro is the same underlying model as GPT-5.6 Luna, served with `reasoning.mode` set to `pro`" and Claude "-fast" variants are "identical capabilities with higher output speed at 2x pricing." OpenAI's API docs confirm pro mode is a request parameter billed at the same token rates ("Pro mode performs more model work… increasing token usage and cost"). Legacy Pros (o1-pro, gpt-5-pro, 5.4/5.5/5.2-pro) are genuinely distinct products (6–10× pricing, no "same underlying model" language) and stay separate. Rule: a variant whose catalog description contains "same underlying model as \<sibling\>" or "identical capabilities" collapses into its sibling before any join — inheriting base identity, pricing basis, and (later) score; `canonical_slug` does NOT mark equivalence (verified: luna-pro has its own canonical slug); the parser extracts the linked sibling from the description markdown link. Collapse list is logged in every build report (D4 audit trail). Consequence: the B10 fake-runner-up case (Luna/Luna-Pro tie) dissolves by construction. | A vendor description changes (e.g. "-fast" becomes a real distinct model) → the description parser stops matching and the variant correctly remains a separate row; re-verify if a collapsed variant reappears with distinct specs. |
| D16 | 2026-08-30 | D14's revisit trigger fired: B12 coverage 59.52% (125/210) vs the 60% gate. | (a) Accept 59.52% and amend the gate; (b) bounded miss-audit (~10 sampled misses) before deciding; (c) reopen D14's source choice. | (a) Accept 59.52%; D14's <60% reopen trigger is amended to a review-at-next-source-probe note. | User decision 2026-08-30. The gate existed to prevent a starved pair card; that risk is gone (125 scored vs 19 pre-B12, 6.6×; full GPT-5.6 family correctly resolved). The fallback "reopening" points to is dead — cross-platform non-comparability (r=0.794, mean Δ−20.1) killed every hybrid — so (c) has no viable destination and is recorded as closed. Misses are concentrated in low-priority families (qwen ×21, gpt ×13, mistral ×8) and most appear genuinely unscored in AA's free tier; a bounded hardcode-audit remains available as ordinary backlog work (see B14 Later), not as a gate violation. | If coverage falls materially (~≤55%) or the planning-slot candidate pool shrinks below usable, re-probe sources. |
| D17 | 2026-08-30 | D15's material-difference guard retained the Claude `-fast` variants as separate rows (their `supported_parameters` differ from the base — strictly subtractive: fast mode loses `max_completion_tokens`/`temperature`), leaving them unscored. Are they distinct models, collapsible, or something in between? | (a) Keep the guard's verdict fully (unscored separate rows); (b) amend D15 to collapse them (recommended then); (c) keep rows separate but inherit intelligence from the base. | (c) Separate rows, inherited intelligence. | User decision 2026-08-30: "the intelligence is the same, the 'fast' is on the operational side — higher prio on the server → faster thinking/response." Vendor descriptions assert capability identity verbatim ("identical capabilities with higher output speed at 2x pricing"); the parameter delta is subtract-only (same engine, fewer knobs), and pricing is a clean tier ratio — the intelligence claim is vendor-validated even though the serving mode differs. Rule: a variant retained as a separate row by the material-difference guard may inherit its linked base's score ONLY when (i) the description affirmatively states capability identity ("identical capabilities" / "same underlying model") with an extractable sibling link, (ii) the base has a non-null score, (iii) the difference is subtract-only or price/speed-tier only. Labeled `intelligence_scope: "variant-inherited"` + `inherit_from: <base_id>`, logged in the collapse/skip audit. Legacy Pros excluded (distinct SKUs, D15/D14). Side note recorded: `claude-opus-4.7-fast` is a "premium 6x pricing" tier, not 2x — its row stays separately priced on the scatter. | Vendor retires fast-mode or rewords descriptions → parser stops matching, rows revert to unscored (safe default). |
| D18 | 2026-08-30 | B1 usability trial 1 FAILED: the recommended pair (DeepSeek V4 Flash 0423 → 0731) put a rank-24/125 model (51.8) in the planning slot, ~11 points below the frontier (max 63.1) at ~1/125th of the price. Root cause: D2's "planning ≥ p75 intelligence" was written pre-B12 (19 scored models), where p75-of-catalog approximated "near frontier"; with 125 scored models the heavily left-skewed distribution makes p75-of-catalog = 43.3 admit mid-catalog models. What should the planning floor be? | (a) Frontier band: max − 8 points; (b) p90 of scored models; (c) keep p75-of-all; (d) fixed absolute threshold. | (a) Frontier band, max − 8 — AND a visible adjustable control (user: "visibleA"). | User decision 2026-08-30 ("A … visible"). Relative bands survive AA index-version re-basing (absolute thresholds rot on 4.1 → 5.0 rescales); an 8-point band at current data admits ~10–16 genuine frontier models (opus-5, fable-5, grok-4.6, sol, kimi-k3, glm-5.3, qwen3.8-max…). Visibility is D7's principle applied to a second taste parameter: some users want top-3 only, others a wider band; the floor must not be a hidden constant. Execution floor (median) unchanged. | If trial 2 shows the band admits too few planning candidates for a valid pair (honest no-qualifying-pair state fires often), revisit band width; if AA re-bases the index, re-validate band behavior. |
| D19 | 2026-08-30 | Same defect trial: 2 of the top-3 pairs were mirror images of the same twin models (deepseek-v4-flash ↔ deepseek-v4-flash-0731: distinct rows — different context 1M vs 1.31M, different generations, NO capability-identity language — so D15/D17 correctly did NOT collapse them, but they are economically near-identical: same score 51.8, ~same price). The pair algorithm enforces only ID-inequality. Should pairs require economic distinctness? | (a) Separation rule: pair members must differ by ≥1.5× in price OR ≥2.0 intelligence points; (b) separation + same-family top-3 downrank; (c) status quo. | (a) Separation rule only. | User decision 2026-08-30 ("yes"). Mirror-pairs of economically-identical models present one recommendation twice and consume top-3 slots that should show real alternatives; the rule is deterministic and testable, killing twin mirror-pairs by construction while leaving legitimate near-frontier + cheap-execution pairings untouched. Same-family downrank rejected for now: it is a preference, not a constraint, and would need its own evidence. Verified the twins are NOT a D15 case (no "same underlying model" description; re-post-trained revision). B10's reopen clause anticipated exactly this; B10 re-closed upon D19 landing. | If users legitimately want twin-model pairs (unlikely) or near-identical distinct models dominate a price tier, revisit threshold values. |
| D20 | 2026-08-30 | B14's hardcode pass: the brief's 10 suggested candidates (claude-sonnet-4.5, claude-opus-4.1, claude-haiku-4.5, gemini-3.1-flash-lite, deepseek-chat-v3.1, deepseek-v3.2-exp, qwen3-coder-plus/flash, codestral-2508, gpt-5.1-codex-max) all 404 on AA's web pages, and the AA detail API (`/api/v2/language/models/{slug}`) returns 403 for the Free-tier key (verified; Pro+ only). The Free-tier list endpoint — the only citable AA data — contains none of them. Which miss-table models are citable with confirmed identity? | (a) Hardcode only models with a citable primary source (AA web page with matching identity); (b) relax the citation rule to near-named siblings; (c) skip the hardcode pass entirely. | (a) Hardcode only citable, identity-confirmed models — landed 2 of a possible 10. | Verified against AA web pages for every miss-table model: only `qwen/qwen3.6-max-preview` (41.1, AA `qwen3-6-max`) and `google/gemini-3-flash-preview` (38.7, AA `gemini-3-flash-reasoning`) are citable with confirmed identity AND above the pair-card execution floor (median 32.1). Near-named siblings (e.g. `qwen3-8-flash-next` for `qwen3.8-flash`, `deepseek-v3-1` for `deepseek-chat-v3.1`) are different models — citing them would violate D4's "wrong beats invented" rule. All other miss-table models score below the execution floor or are legacy generations (cosmetic coverage, D4-forbidden). Both landed overrides use the AA web value (index 4.1.1) with the version mismatch vs the capture (4.1) recorded in `note`. | A Pro AA key unlocks the detail endpoint and likely makes planning-slot candidates (claude-sonnet-4.5, gpt-5.1-codex-max) citable — revisit then. |
| D20 | 2026-08-30 | Usability trial 2: mechanics trusted (frontier-band planning pick, D19 working, live-data math verified) but the card "feels one-note" — all 3 rows were glm-5.3-flash planners over DeepSeek executors. How should the pair card diversify its presentation without breaking the ranking contract? | (a) 3 archetypes by price steps (user idea): cheapest/cheapest, pricier+more-capable planner, cheaper planner + step-up executor; (b) lens card (Option S): 1 cheapest qualifying pair + 2 purpose-labeled lenses (planning step-up, execution step-up), each lens the argmin of its own objective; (c) tiered ranking with rejection explanations. | (b) Lens card, with a family-diversity guard on the planning lens + a collapsible true-ranking view. | User decision 2026-08-30: "proceed with your recommendation." Data grounded the choice: the frontier band spans only 7.8 pts (55.3–63.1), so (a)'s "more capable planner" as-proposed yields a +0.2-pt/25×-price pseudo-alternative (qwen3.8-2.4t-a95b 57.7  vs glm-5.3-flash 57.5 /bin/zsh.16) — an upgrade in name only. Purpose-lenses answer three DIFFERENT questions (minimize spend / what if planning quality is the bottleneck / what if execution quality is), each provably optimal for its own objective, so nothing is misranked; labels stay honest. Guard: planning-lens model must be ≥Δ 3 intelligence points above the cheapest planner AND a different family (Δ=3 lands on kimi/sol/opus class, not glm/qwen rounding). Execution lens: cheapest planner + highest-intelligence executor within a 10× executor-price ceiling over the cheapest executor (prevents runaway opus-5-fast/fable-5  picks when  grok-4.6 exists). D19, floors, and the expected-$ ranking of the recommended pair are unchanged; runners-up 2/3 are REPLACED by lenses (honestly labeled), true rank-2/3 stay accessible via the toggle. | If the same model appears in all three pairs repeatedly (lenses lose diversity), add a cross-lens family-diversity preference; if users can't articulate what question each lens answers, copy/UX fix first, rule change second. |
| D21 | 2026-08-30 | Trial-2 follow-up: B16's three lenses all optimize cost under constraints; nothing on the card shows what money BUYS. Add a 4th row — most capable planning + most capable execution regardless of cost — as a contrast/anchor? | (a) Add as a co-equal 4th strategy; (b) add as a visually demoted 'capability ceiling' reference row; (c) don't add (scatter already shows premium models). | (b) Capability-ceiling reference row. | User decision 2026-08-30 ("proceed with your recommendation"). A cost decision needs its counterfactual: the ceiling pair makes the optimization's value visible as data (current: opus-5 → fable-5, ~.75/1M vs the /bin/zsh.1411 anchor = 133× for +5.6 planning/+10.3 execution points — that sentence is the product thesis). Fits D20's argmin/argmax contract (each row provably optimal for its declared objective). Co-equal treatment rejected: D3 ranks by expected-$; the card must not hedge its own thesis. Definition: planning = argmax intelligence (tie → cheapest); execution = argmax intelligence excluding the planner itself AND its D15/D17 variant children (inherit_from) — without the exclusion the naive pick is opus-5-fast (63.1 ), the same model as the planner at 2× price, i.e. one model playing both roles. Note the recorded tension with D20: lens 3 has a 10× ceiling, lens 4 deliberately none — ceilings are per-lens, defined by each objective. Floors/D19 trivially satisfied (argmax passes floors; opus-5/fable-5 gap 1.0 pt but ratio 2.0× passes D19 via price path). Visual demotion: labeled CEILING, shows the vs-anchor multiple. | If the ceiling pair is always same-family (it currently is — Anthropic), watch whether it still reads as useful contrast vs. noise; if D17 inherit graphs make the exclusion rule ambiguous, refine. |
| D22 | 2026-08-30 | B16 shipped the 4-row lens card (verified; trial-3 read positive apart from the ceiling-execution question, settled by data: opus-5 out-ranks fable-5 on all three held dimensions — general 63.1 vs 62.1, coding 78 vs 76.5, agentic 59.2 vs 56.6 — at half the price; no rule change). User then proposed single-vendor stack rows (Anthropic opus→sonnet, OpenAI sol→terra). Data check: vendor-argmax would duplicate the ceiling row for Anthropic (opus→fable, .75); the user's opus→sonnet instinct is actually best-planner + cheapest-qualifying-executor, which the rule reproduces (.55); the same rule overrules the OpenAI prior (sol→luna .11, not sol→terra .00 — terra is 10× luna's price for +4.3 pts). Agent initially scoped vendor lenses as collapsed-only; user pushback: vendor lenses top-level, ALL other rows except minimize-spend demoted to collapsibles. Final card architecture? | (a) 4-row lens card + collapsed vendor section (agent rec); (b) user layout: minimize-spend + 2 vendor rows top-level; lenses/ceiling/ranking collapsed with headline numbers visible in collapsed chrome; (c) vendor rows as hand-pinned brand pairings. | (b) Top-level: minimize spend + vendor lens rows; collapsed: ceiling, bottleneck lenses, ranking view — each collapsed section shows its headline number. Vendors selected by RULE: rank vendors by best floor-qualifying planner's intelligence; top 2 that can field a floor+D19-legal pair get top-level rows (today: Anthropic .55 / 82×, OpenAI .11 / 29×). No hardcoded brand lists. **Tie-break (user-picked option ii):** vendors tied on best-planner intelligence (today: x-ai and openai both 60.9) are broken by execution depth — the count of floor-qualifying models the vendor can field (OpenAI 5+ vs x-ai 2) — rewarding vendors with a real execution-tier lineup, which is the point of a vendor lens. | User decision 2026-08-30 ("yes both", after pushback). The vendor constraint (one bill, one API key, one relationship) is a first-class real-world constraint for the D1 persona — arguably more decision-relevant than the exploratory bottleneck lenses; trial evidence outranks the agent's original design. Rule-based selection survives model churn and corrects brand priors (sonnet-5 emerges for Anthropic; luna over terra for OpenAI — measured data beats marketing positioning, same principle as the fable call). Vendor rows are labeled CONSTRAINT VIEWS, not strategies: the recommended answer remains minimize-spend; no fabricated substitute for vendors that can't field a legal pair (honest absence, e.g. x-ai: two frontier models, same price, Δ0.2 → fails D19 → 'no qualifying pair' in expanded view only). Headline-number-in-collapsed-chrome guardrail preserves the anchor↔ceiling contrast (.75 · 133×) at zero top-level row cost. | If vendor rows converge to the same pair as Row 1 (possible if a single vendor dominates the frontier), re-examine; if users misread vendor rows as recommendations rather than constraint views, rework labeling. |